# WORKFLOW: Environment variables template for Trade Compliance API configuration.
# Used by: Development setup, production deployment, configuration management
# Configuration categories:
# 1. Database settings (PostgreSQL connection)
# 2. Vector search settings (Qdrant, embedding models)
# 3. LLM settings (Ollama, model names)
# 4. Security settings (JWT, API keys)
# 5. Classification thresholds (confidence, margin)
# 6. API settings (CORS, rate limiting)
# 7. Monitoring and logging configuration
#
# Setup flow: Copy env.example -> .env -> Configure values -> Start services
# This ensures consistent configuration across environments.

# Database Configuration
DATABASE_URL=postgresql://tarco:tarco@postgres:5432/tarco

# Redis Configuration
REDIS_URL=redis://redis:6379

# LLM Configuration
OLLAMA_URL=http://ollama:11434
LLM_MODEL=llama2:7b

# Vector Search Configuration
EMBEDDING_MODEL=BAAI/bge-m3
RERANKER_MODEL=BAAI/bge-reranker-base
VECTOR_DIMENSION=1024
TOP_K_RETRIEVAL=32
TOP_K_RERANK=5

# Classification Configuration
CONFIDENCE_THRESHOLD=0.62
MARGIN_THRESHOLD=0.07

# Security Configuration
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=Trade Compliance API
VERSION=1.0.0

# Environment Configuration
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# CORS Configuration
ALLOWED_ORIGINS=["*"]
ALLOWED_METHODS=["*"]
ALLOWED_HEADERS=["*"]

# Rate Limiting
RATE_LIMIT_PER_MINUTE=100

# Monitoring Configuration
ENABLE_METRICS=true
METRICS_PORT=8001

# Qdrant Configuration
QDRANT_URL=http://qdrant:6333
